{
  "name": "flipdot",
  "version-string": "0.1.0",
  "description": "Dependencies for building the Flipdot FFGL plugin. GLEW is only needed on Windows; macOS uses the system OpenGL framework. There is nothing else: the discs and the driver are solved in Sign.cpp, and the only audio that reaches the plugin is the host's own FFT buffer.",
  "dependencies": [
    {
      "name": "glew",
      "platform": "!osx"
    }
  ]
}
